{
  "name": "rebot-arm-webui",
  "version": "0.2.1",
  "type": "module",
  "scripts": {
    "dev": "npm run build && ./scripts/start.sh",
    "start": "npm run build && ./scripts/start.sh",
    "build": "vite build",
    "preview": "npm run start",
    "start:8001": "npm run build && PORT=8001 ./scripts/start.sh",
    "install:local": "./scripts/install.sh"
  },
  "dependencies": {
    "@iconify/react": "^6.0.2",
    "three": "^0.165.0",
    "urdf-loader": "^0.12.6",
    "vite": "^5.4.0"
  }
}
